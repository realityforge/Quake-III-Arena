# TODO: Why not switch to zlib's inflate rather than using the slower puff?
cc_library(
    name = "puff",
    srcs = [
        "puff.c",
    ],
    hdrs = [
        "puff.h",
    ],
    includes = ["."],
    visibility = ["//code/renderercommon:__pkg__"],
)
